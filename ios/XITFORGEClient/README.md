# XITFORGE iOS client scaffold

Before building, replace `https://YOUR-DOMAIN.example` in `WebViewController.m`
with the public URL of the XITFORGE server.

This scaffold only reads the XITFORGE configuration API.
